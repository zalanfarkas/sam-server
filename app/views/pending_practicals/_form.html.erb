<%= form_with(model: pending_practical, local: true) do |form| %>
  <% if pending_practical.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pending_practical.errors.count, "error") %> prohibited this pending_practical from being saved:</h2>

      <ul>
      <% pending_practical.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :raspberry_pi_id %>
    <%= form.text_field :raspberry_pi_id, id: :pending_practical_raspberry_pi_id %>
  </div>

  <div class="field">
    <%= form.label :practical_id %>
    <%= form.text_field :practical_id, id: :pending_practical_practical_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
